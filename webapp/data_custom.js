// ============================================================
// FAANG Prep 2026 — AI Curated Job Custom Roadmaps
// ============================================================
// Each job's roadmap lives in its own file under custom_paths/,
// which appends its entry to DATA_CUSTOM.paths. This file only
// declares the shared container so per-job files load after it
// (see index.html) and stay independently readable/editable.

const DATA_CUSTOM = {
  paths: {}
};
